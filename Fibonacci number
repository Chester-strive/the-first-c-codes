#include<stdio.h>//描述第n个斐波那契数 
int Fib(int n)
{
	if(n<=2)
	{
		return 1;
	}
	else
	{
		return Fib(n-1)+Fib(n-2);
	}
}
int main()
{
	int n,num;
	scanf("%d",&n);
	num = Fib(n);
	printf("%d",num);
	return 0;
}
